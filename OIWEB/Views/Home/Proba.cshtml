@model IEnumerable<OIWEB.Clanci>

@{
    ViewBag.Title = "Proba";
}

<h2>Proba</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tip)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Datum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDSBK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDKorisnik)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tip)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Datum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDSBK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDKorisnik)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDClanak }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDClanak }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDClanak })
        </td>
    </tr>
}

</table>
